#include <iostream>
using namespace std;
main()
{
    int i,n,sum;
    sum=0;
    cout<<"adad ra vared konid:";
    cin>>n;
    for(i=1;i<=n;i++)
       if (n%i==0) sum+=i;
       cout<<"majmoo="<<sum;
}
